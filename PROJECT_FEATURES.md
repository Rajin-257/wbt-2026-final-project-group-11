23-50257-1: 
Fetures:
        MVC Framework
            Developed Mvc Formate
        Authentication
            Login (Client Side + Server Side Validation)
            Registration (Client Side + Server Side Validation)
            Logout
        Profile Management:
            Can Read profile
                Update Profile
        Home
            Medicine browsing ( Categories, Search & Filtering (AJAX) )

23-50297-1 
Fetures:
        Medicine Management
            full CRUD
        Category Management
            full CRUD
        Customer 
            Delete Customers
            View All Customers
        Purchase Requests:
            Read All purchase request
            Accept/Reject Orders
        Purchase History
            Can Read Only

23-54596-3  
Fetures:
        Customer Journey 
            Add to Cart
            Cart Management (increase/decrease quantity, remove)
            Checkout with Address
            Invoice
            Payment Method Selection 
            Order Confirmation

project Feature 
    SQL injection prevention
    XSS protection (output encoding via `e()`, `e_url()`, `e_css()`; security headers; client-side `public/xss.js` for AJAX-rendered HTML)
    Password hashing: PHP built-in `password_hash()` / `password_verify()`
